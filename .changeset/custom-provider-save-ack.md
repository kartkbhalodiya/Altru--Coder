---
"altru-coder-agent-by-kartik": patch
---

Keep provider API-key and Add Model save flows from staying stuck on Saving when provider refresh or config invalidation is slow after credentials are saved. Avoid trusting provider model-list reasoning metadata during model fetches, include NVIDIA Kimi K2.6 thinking presets by default, and keep NVIDIA setup off the slow all-model fetch path unless the user manually requests it.
